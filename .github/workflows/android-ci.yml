name: Android CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 防止 gradle wrapper 被篡改（安全&稳定）
      - name: Validate Gradle Wrapper
        uses: gradle/wrapper-validation-action@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # 额外的 Gradle 缓存（可选，但保留也不坏）
      - name: Gradle cache
        uses: gradle/gradle-build-action@v3

      - name: Ensure Gradle wrapper is executable
        run: chmod +x ./gradlew

      # 防止有人把 apk/png/jar 等二进制误提交进仓库（你平台会封）
      - name: Reject committed binaries
        shell: bash
        run: |
          set -euo pipefail
          bad=$(git ls-files | grep -E '\.(apk|aab|jar|jks|keystore|png|jpg|jpeg|webp|so|dex)$' || true)
          if [ -n "$bad" ]; then
            echo "Binary files detected in repo (must not be committed):"
            echo "$bad"
            exit 1
          fi

      - name: Gradle version
        run: ./gradlew --version

      - name: Assemble Debug
        run: ./gradlew clean assembleDebug

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: EncryptionChat-debug-apk-${{ github.run_number }}
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: EncryptionChat-reports-${{ github.run_number }}
          path: |
            app/build/reports/**
            app/build/test-results/**
          if-no-files-found: warn
